# README

This README would normally document whatever steps are necessary to get the
application up and running.

Things you may want to cover:

* Ruby version :2.7.5 must installed this version of ruby

* System dependencies: run bundle install to install all system dependencies.

* Configuration: Rails 6 with postgresql.

* Database creation: install pg gem with brew install postgressql.Configure database.yml with password sardarHammad007 and with user name Hammad.

* Database initialization: follow the above instruction and if you still confused read Digital ocean blog for configure database with postgress for solution.

* How to run the test suite:

* Services (job queues, cache servers, search engines, etc.).Stripe is used and it will be automatically installed by bundle install

* Deployment instructions: use gem figaro that will create a application.yml and paste all your credentials there.after then open heroku setting and Use all environment variables credentials there. like API KEYS etc.If you are new in coding watch a toutorial.How to deploy a app in heroku rails.

* ...
